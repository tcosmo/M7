## Horizontal scroll is annoying

## Slight annoying case when scrolling with auto-scroll on

when we've reached bottom of score and scrolling up it will vibrate and prevent scrolling because most likely cursor is still interacting with trigger line

## Settings.json

- make settings use words instead of numbers, e.g. "layoutMode": 2 to "layoutMode": "continuous_vertical"
